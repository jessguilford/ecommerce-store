import { classyPrice } from 'ecommerce-store/helpers/classy-price';
import { module, test } from 'qunit';

module('Unit | Helper | classy price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classyPrice([42]);
  assert.ok(result);
});
